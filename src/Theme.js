import { createTheme } from "@mui/material";


const Theme = createTheme({
    palette:{
        primary:{
            main: "#9c27b0",
            secondary:"#0288d1"
        }
    }
});

export default Theme ;